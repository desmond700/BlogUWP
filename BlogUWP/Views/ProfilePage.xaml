<Page
    x:Class="BlogUWP.Views.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlogUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <RelativePanel Grid.Row="0" x:Name="header" VerticalAlignment="Center" >
            <RelativePanel.Background>
                <AcrylicBrush TintColor="#CCB0D1E8"/>
            </RelativePanel.Background>
            <Image HorizontalAlignment="Center" Height="auto" Margin="50,0,0,0" RelativePanel.AlignVerticalCenterWithPanel="True" Width="auto" Source="/Assets/Square150x150Logo.png"/>

        </RelativePanel>
        <RelativePanel Grid.Row="1" HorizontalAlignment="Stretch" >
            <TextBlock RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="0,251,0,0" Text="Profile Page" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="18"/>

        </RelativePanel>

    </Grid>
</Page>
